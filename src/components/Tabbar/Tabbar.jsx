import TabbarContainer from './Tabbar.styled';

const Tabbar = () => {
	return (
		<TabbarContainer>
			<button>SAVED</button>
			<button>GOING</button>
		</TabbarContainer>
	);
};

export default Tabbar;
